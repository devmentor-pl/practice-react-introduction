import React from 'react'
import Menu from './Menu'

export default class Nav extends React.Component {
    render() {
        return (
            <nav>
                <Menu />
            </nav>
        )
    }
}